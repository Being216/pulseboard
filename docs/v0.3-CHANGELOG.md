# v0.3 变更说明

- 任务按状态拆为「进行中 / 待办 / 受阻 / 已完成」四组，空组也会显示。
- 每组改为可折叠 section，折叠状态保存在 `localStorage`；默认展开进行中和待办。
- 进行中按 `updated_at` 降序，已完成按完成时间字段降序；兼容 `done/completed`、`pending/todo` 状态别名。
- 沿用 v0.2 任务卡片及编辑、取消、刷新、提醒操作，后端 API 不变。
